{{#if files}}
<h3 class="etched">Data Statistics</h3>
<ul class="propertyList">
	<li><span class="label">Sessions:</span>&nbsp;<span class="value">{{numSessions}}</span>
		<button class="listAllSessions translucent tiny symbolic"
				title="List all sessions">&equiv;</button>
	</li>
	<li><span class="label">Geol. Results:</span>&nbsp;<span class="value">{{numResults}}</span></li>
	{{#if hasNetwork}}
	<li><span class="label">Sites:</span>&nbsp;<span class="value">{{numSites}}</span></li>
	<li><span class="label">Sectors:</span>&nbsp;<span class="value">{{numSectors}}</span></li>
	{{/if}}
	<li><span class="label">Files:</span>&nbsp;{{#if files}}<span class="value">{{files.length}}</span>{{/if}}</li>
	{{#each files}}
		<li class="fileitem" title="{{name}}">{{name}}</li>
	{{/each}}
</ul>
{{/if}}
